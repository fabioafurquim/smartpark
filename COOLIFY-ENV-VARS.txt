# ========================================
# COOLIFY ENV VARS TEMPLATE
# SmartPark production placeholders only
# ========================================

# Never commit real credentials to this repository.
# Configure the real values directly in Coolify.

DATABASE_URL=postgresql://postgres:CHANGE_ME@POSTGRES_HOST:5432/smartpark
NEXTAUTH_URL=https://smartpark.seu-dominio.com
NEXTAUTH_SECRET=GENERATE_WITH_OPENSSL
NODE_ENV=production
HOSTNAME=0.0.0.0

# Generate NEXTAUTH_SECRET with:
# openssl rand -base64 32
